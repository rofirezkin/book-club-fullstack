# Copy this file to `.env` and adjust the values for your local setup.

# Database connection string used by Prisma.  When using docker-compose your
# database runs on localhost:5432 with the credentials defined in the
# docker-compose.yml.  The `schema=public` query parameter ensures that
# migrations target the public schema.
DATABASE_URL="postgresql://bookclub_user:bookclub_pass@localhost:5432/bookclub?schema=public"

# The port on which the API server will listen.
PORT=3001